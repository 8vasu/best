# Cargo build output
/target

# Cargo lock (omit for binaries if you prefer not to pin deps in CI)
# Cargo.lock

# Generated by flex and bison (written to OUT_DIR during cargo build,
# but guard against any accidental local generation)
lex.yy.c
y.tab.c
y.tab.h
